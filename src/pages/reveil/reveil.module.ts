import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ReveilPage } from './reveil';

@NgModule({
  declarations: [
    ReveilPage,
  ],
  imports: [
    IonicPageModule.forChild(ReveilPage),
  ],
})
export class ReveilPageModule {}
